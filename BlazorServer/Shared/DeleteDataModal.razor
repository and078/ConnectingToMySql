@using BlazorServer.Models

<div class="simple-form ">
    
    <button @onclick="SubmitForm" class="btn btn-danger">Delete</button>
    <button @onclick="Cancel" class="btn btn-secondary">Cancel</button>
</div>

@code {

    [CascadingParameter]
    BlazoredModalInstance BlazoredModal { get; set; }

    [Parameter]
    public PersonModel person { get; set; }


    protected override void OnInitialized() => BlazoredModal.SetTitle("Remove item");

    async Task SubmitForm() => await BlazoredModal.CloseAsync(ModalResult.Ok<PersonModel>(person));
    async Task Cancel() => await BlazoredModal.CancelAsync();
}